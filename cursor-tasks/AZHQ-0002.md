TASK CODE: AZHQ-0002
DATE: 2026-07-05
TIME: 07:05 BST

PROJECT: AZ-HQ (task tracker + ledger system)
REPO: Az1341/project-tracker
GOAL: Fix everything AZHQ-0001 found broken: flatten the folder structure, replace the 5 scripts with corrected versions (path bug fixed), initialize git properly, install BurntToast, and register the daily scheduler.

CONSTRAINTS:
- Root folder stays at C:\project-tracker
- Do not lose any data already in tasks.json or ledger.json — merge, don't overwrite
- The corrected script files are attached in this task's folder (replace, don't hand-edit)

PLAN:
1. Move C:\project-tracker\ledger-update\ledger.json to C:\project-tracker\ledger.json (root)
2. Move C:\project-tracker\ledger-update\Add-Expense.ps1 and Ledger-Summary.ps1 to root (overwrite with the corrected versions provided instead, since they fix the path bug too)
3. Move C:\project-tracker\azhq-update\cursor-tasks\ to C:\project-tracker\cursor-tasks\ (root)
4. Delete the now-empty ledger-update\ and azhq-update\ folders
5. Replace Check-Tasks.ps1, Mark-Done.ps1, Setup-Scheduler.ps1 at root with the corrected versions provided (path fixed to C:\project-tracker)
6. Initialize git: if https://github.com/Az1341/project-tracker.git already has commits, run `git clone` into a temp folder and merge; if it's empty, run `git init`, `git remote add origin https://github.com/Az1341/project-tracker.git`, `git add .`, `git commit -m "Fix paths, flatten structure"`, `git branch -M main`, `git push -u origin main`
7. Run `Install-Module -Name BurntToast -Scope CurrentUser` (confirm Y if prompted about untrusted repository)
8. Run `.\Setup-Scheduler.ps1` to register the daily task
9. Run `.\Check-Tasks.ps1` once manually to confirm it now works end to end
10. Report back pass/fail on each of the 9 files present at root, git remote confirmed, Check-Tasks.ps1 runs clean, scheduled task registered

FILES TO TOUCH: ledger.json, Add-Expense.ps1, Ledger-Summary.ps1, Check-Tasks.ps1, Mark-Done.ps1, Setup-Scheduler.ps1, cursor-tasks\ (move only)

ACCEPTANCE: All 9 required files present at C:\project-tracker root (not in subfolders), git remote confirmed pointing to Az1341/project-tracker, Check-Tasks.ps1 runs without error and shows a notification, ProjectTrackerDailyCheck scheduled task confirmed registered and enabled.

MODE: Fix and report. If git already has remote history that conflicts with local files, stop and report back before force-pushing anything.
