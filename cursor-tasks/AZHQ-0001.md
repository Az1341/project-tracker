TASK CODE: AZHQ-0001
DATE: 2026-07-05
TIME: 06:33 BST

PROJECT: AZ-HQ (task tracker + ledger system)
REPO: Az1341/project-tracker
GOAL: Confirm the local root folder structure is correct and complete on this machine, and verify the daily scheduler + git sync are working end to end.

CONSTRAINTS:
- Root folder is C:\project-tracker (already created by Ahmad — do not create a duplicate elsewhere)
- Do not rename the GitHub repo or folder
- Do not modify tasks.json or ledger.json content — structure check only

PLAN:
1. Confirm these files exist in C:\project-tracker: tasks.json, ledger.json, Check-Tasks.ps1, Mark-Done.ps1, Add-Expense.ps1, Ledger-Summary.ps1, Setup-Scheduler.ps1, README-SETUP.md, cursor-tasks/INDEX.md
2. Confirm git remote points to https://github.com/Az1341/project-tracker.git
3. Run Check-Tasks.ps1 once manually to confirm it pulls, reads dates correctly, and (if BurntToast is installed) shows a notification
4. Confirm the daily scheduled task "ProjectTrackerDailyCheck" exists in Windows Task Scheduler and is enabled
5. Report back which of the above passed/failed — do not fix silently, flag anything missing

FILES TO TOUCH: none (verification only, no edits)

ACCEPTANCE: All 9 files confirmed present, git remote confirmed correct, Check-Tasks.ps1 runs without error, scheduled task confirmed registered and enabled.

MODE: Verify only. No changes without reporting back first.
